[build]
  command = "npm run build"
  publish = "out"
  functions = "netlify/functions"

# xlsx 호환 모드 시트(Apps Script를 못 붙이는 경우)를 위한 주기적 동기화 — 5분마다.
[functions."sheet-sync-scheduled"]
  schedule = "*/5 * * * *"
